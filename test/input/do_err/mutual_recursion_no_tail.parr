def is_even(x):
    if x == 0:
        printStack(true)
    else:
        is_odd(x - 1)

def is_odd(x):
    if x == 0:
        printStack(false)
    else:
        is_even(x - 1)

is_even(8) && is_odd(3)
